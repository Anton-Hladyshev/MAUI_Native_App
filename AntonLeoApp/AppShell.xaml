<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AntonLeoApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AntonLeoApp"
    Title="AntonLeoApp"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage"
            Icon="home.png" />

        <ShellContent
            Title="Characters"
            ContentTemplate="{DataTemplate local:CharactersPage}"
            Route="CharactersPage"
            Icon="charecter.png" />

        <ShellContent
            Title="New Character"
            ContentTemplate="{DataTemplate local:UserIOPage}"
            Route="UserIOPage"
            Icon="plus.png" />

        <ShellContent
            Title="Hologram"
            ContentTemplate="{DataTemplate local:HologramPage}"
            Route="HologramPage"
            Icon="hologram.png"/>
    </TabBar>

</Shell>
